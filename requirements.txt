# Python requirements for gesture toggle server
# Pinned versions selected for broad compatibility with Python 3.10/3.11 on Linux,
# and mutual compatibility between OpenCV, MediaPipe, numpy, websockets, and pynput.

# Core numerical stack
numpy==1.26.4

# Computer vision
opencv-python==4.9.0.80

# Hand gesture detection/landmarks
mediapipe==0.10.14

# Async WebSocket server/client
websockets==12.0

# Keyboard/mouse control hooks
pynput==1.7.6
